# ls
alias ls="eza --color=always --icons --group-directories-first"
alias la="ls -a"
alias ll="ls -l"
alias lla="ls -la"
alias lt="ls --tree --level=2"

# utils
alias mkdir="mkdir -p"
alias rt="exec fish"
alias cat="bat --paging never --plain --theme gruvbox-dark"
